<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Just Applause Runway</title>
  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(to bottom, #000, #111);
      color: white;
      overflow-x: hidden;
    }

    header {
      text-align: center;
      padding: 2rem;
      font-size: 2rem;
      font-weight: bold;
      letter-spacing: 4px;
      text-transform: uppercase;
    }

    .stage {
      position: relative;
      width: 100%;
      height: 100vh;
      overflow: hidden;
      background: radial-gradient(circle at top, #333, #000);
    }

    /* Spotlight effect */
    .spotlight {
      position: absolute;
      top: 0;
      left: 50%;
      width: 300px;
      height: 100%;
      background: radial-gradient(ellipse at top, rgba(255,255,255,0.3), transparent 70%);
      transform: translateX(-50%);
      pointer-events: none;
    }

    /* Runway path */
    .runway {
      position: absolute;
      bottom: 0;
      left: 50%;
      width: 60%;
      height: 200px;
      background: linear-gradient(to top, #222, #111);
      transform: translateX(-50%) perspective(800px) rotateX(25deg);
      box-shadow: 0 0 40px rgba(255,255,255,0.2) inset;
      border-left: 5px solid #444;
      border-right: 5px solid #444;
    }

    /* Product images walking */
    .model {
      position: absolute;
      bottom: 200px;
      left: -200px;
      width: 200px;
      animation: walk 12s linear infinite;
    }

    .model img {
      width: 100%;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(255,255,255,0.3);
    }

    @keyframes walk {
      0% { left: -200px; opacity: 0; }
      10% { opacity: 1; }
      50% { left: 50%; transform: translateX(-50%); opacity: 1; }
      90% { opacity: 1; }
      100% { left: 120%; opacity: 0; }
    }

    footer {
      text-align: center;
      padding: 1rem;
      background: #111;
      color: #aaa;
    }
  </style>
</head>
<body>
  <header>✨ Just Applause ✨</header>

  <div class="stage">
    <div class="spotlight"></div>
    <div class="runway"></div>

    <!-- Models walking with your clothes -->
    <div class="model" style="animation-delay: 0s;">
      <img src="https://via.placeholder.com/200x300/ffffff/000000?text=Look+1" alt="Look 1">
    </div>
    <div class="model" style="animation-delay: 6s;">
      <img src="https://via.placeholder.com/200x300/cccccc/000000?text=Look+2" alt="Look 2">
    </div>
  </div>

  <footer>&copy; 2025 Just Applause — Fashion with Applause</footer>
</body>
</html>
